version: '3.8'

services:
  job-service:
    build: 
      context: .
      dockerfile: Dockerfile
    container_name: job-service-container
    ports:
      - "5000:5000"
    env_file:
      - .env
    restart: unless-stopped
